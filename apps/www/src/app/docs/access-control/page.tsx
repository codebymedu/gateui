import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Access Control Setup - GateUI",
  description:
    "Setup access controls in your application using GateUI Identity Management Library.",
};

export default function OrganizationManagementPage() {
  // --- RENDER ---

  return (
    <div className="border-primary border p-8 flex flex-col items-center">
      <h1 className="text-xl font-bold">Access Control is coming soon</h1>

      <p>
        I am currently working in this feature and it will be added in the
        following weeks.
      </p>
    </div>
  );
}
